#include "BlockDescriptor.h"

void BlockDescriptor::render(OpenGLBuffer& targetBuffer, const BlockInfo& blockInfo, const BlockPosition& position, const World& workingWorld) const
{
	/* do nothing for an undefined block */
}
